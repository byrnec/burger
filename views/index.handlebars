<h1>Burgers!</h1>

<h2>Burgers that are devoured!</h2>

<ul>
  {{#each burgers}}
    {{#unless devoured}}
        <li>
	{{burger_name}}

	<button class="change-burger" data-id="{{id}}" data-newburger="true">
		EAT THE BURGER!
	</button>

</li>
    {{/unless}}
  {{/each}}
</ul>

<h2>Burgers that are not devoured!</h2>

<ul>
  {{#each burgers}}
    {{#if devoured}}
      <li>
	{{burger_name}}

	<button class="change-burger" data-id="{{id}}" data-newburger="false">
		MAKE THE BURGER!
	</button>

</li>

    {{/if}}
  {{/each}}
</ul>

<h2>Add a burger</h2>
<form class="create-form">

  <div class="form-group">
    <label for="ca">Burger Name:</label>
    <input type="text" id="new-burger" name="burger-name">
  </div>

  {{!-- <div class="form-group">
    <label for="devoured">devoured?</label><br>
    <input type="radio" name="devoured" value="1" checked> devoured!<br>
    <input type="radio" name="devoured" value="0"> Eaten
  </div> --}}

  <button id = "submit-burger" type="submit">Add Burger</button>
</form>




